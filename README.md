# DDQN-action-encoding

## Get Started

1. Ensure you have Python 3.11+ installed.
2. Install dependencies with `uv sync` (or `pip install -r requirments.txt` if you prefer pip).
3. Run the main script: `uv run python -m src.main`.
